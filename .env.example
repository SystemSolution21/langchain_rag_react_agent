# =============================================================================
# LangChain RAG React Agent - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Application Paths Configuration(Optional- For local development)
# -----------------------------------------------------------------------------
PROJECT_ROOT = 
PDFS_DIR = 
DB_DIR = 
LOGS_DIR =

# -----------------------------------------------------------------------------
# LangSmith Configuration
# -----------------------------------------------------------------------------
LANGSMITH_API_KEY=

# -----------------------------------------------------------------------------
# Ollama Configuration (Local LLM)
# -----------------------------------------------------------------------------
# Ollama base URL - Choose based on your setup:
#
# Option 1: Containerized Ollama (for new team members, CI/CD)
#   Use this if running: docker-compose --profile local up -d
OLLAMA_BASE_URL=http://ollama:11434
#
# Option 2: Local Ollama (for active developers with Ollama installed)
#   Use this if running: docker-compose up -d (without --profile local)
#   Uncomment the line below and comment out the line above:
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# Ollama model to use
# Popular options: llama3.2:3b, llama3.2:1b, gemma2:2b, phi3:mini
OLLAMA_LLM=llama3.2:3b

# -----------------------------------------------------------------------------
# Cloud LLM Providers (Optional - for production)
# -----------------------------------------------------------------------------
# OpenAI
OPENAI_API_KEY=
OPENAI_LLM=gpt-4.1-nano

# Anthropic
ANTHROPIC_API_KEY=
ANTHROPIC_LLM=claude-3-haiku-20240307

# Google
GOOGLE_API_KEY=

# -----------------------------------------------------------------------------
# Application Paths (Auto-configured in Docker)
# -----------------------------------------------------------------------------
# These are automatically set in Docker, but can be overridden
# PROJECT_ROOT=/app
# PDFS_DIR=/app/pdfs
# DB_DIR=/app/db
# LOGS_DIR=/app/logs

# -----------------------------------------------------------------------------
# Advanced Configuration
# -----------------------------------------------------------------------------
# Vector store settings
CHROMA_DB_NAME=chroma_db_pdf_advanced

# Embedding model (HuggingFace)
EMBEDDING_MODEL=BAAI/bge-large-en-v1.5

# Retrieval settings
RETRIEVAL_K=3
RETRIEVAL_TYPE=similarity

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

